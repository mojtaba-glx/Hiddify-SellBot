# Changelog

## Hiddify-SellBot v3.0.0

Stable Release برای جمع‌بندی فیکس‌های پرداخت خودکار، نودسازی، لینک اشتراک هوشمند و ابزار نصب.

### تغییرات اصلی

- پایدارسازی تایید خودکار کارت‌به‌کارت با اپ SMS Verifier و Webhook اختصاصی.
- پشتیبانی امن‌تر از بانک‌هایی که ۴ رقم کارت مبدا را داخل SMS ارسال نمی‌کنند، مثل بلو بانک.
- حفظ منطق دقیق ۴ رقم کارت برای بانک‌هایی که آن را ارسال می‌کنند، مثل بانک خاورمیانه.
- جلوگیری از تایید دوباره SMS استفاده‌شده و کاهش ریسک رسیدهای فیک یا تکراری.
- همگام‌سازی بهتر وضعیت پیامک‌های زرد/سبز داخل اپ بعد از تایید پرداخت توسط ربات.
- بهبود گزارش‌های ادمین برای پرداخت‌های تاییدشده با SMS و مدیریت رسید پیوست‌شده.
- پایدارسازی لینک اشتراک هوشمند، QR، b64 و محاسبه مصرف چندسروره.
- تکمیل فیکس‌های نودسازی، نمایش منوها و عملیات کاربران/سرورها.
- بهبود ابزار نصب، آپدیت امن، `update-force`، AutoStart و SSL Wizard.
- تثبیت نسخه برای استفاده عملیاتی به‌عنوان نسخه پایدار جدید.

## Hiddify-SellBot v2.2.2

Patch Release برای تکمیل نودسازی، پایداری لینک اشتراک هوشمند و رفع چند باگ عملیاتی.

### تغییرات اصلی

- تکمیل و پایدارتر شدن همگام‌سازی نودها و عملیات چندسروره.
- اضافه شدن فیکس موقت قابل تنظیم برای مشکل قطع شدن کاربر تازه‌ساخته‌شده در بعضی نسخه‌های Hiddify.
- بهبود لینک اشتراک هوشمند با دریافت کانفیگ نودها توسط User-Agent مرورگر.
- پشتیبانی بهتر از لینک‌های `b64` و ساخت QR داخلی بدون وابستگی به سرویس خارجی.
- حذف وضعیت‌های فیک نود از لینک هوشمند و نگه داشتن فقط وضعیت خود ربات.
- مرتب شدن لیست کاربران از قدیمی‌تر به جدیدتر.
- تمیزتر شدن منوی دامنه‌های نود و جلوگیری از نمایش preview لینک‌ها.
- افزایش ضداسپم بخش خرید به ۸ ثانیه برای جلوگیری از ثبت درخواست‌های پشت‌سرهم.

## Hiddify-SellBot v2.2.1

Patch Release برای بهبود ظاهر ربات و اضافه شدن مدیریت دکمه‌های رنگی تلگرام.

### تغییرات اصلی

- اضافه شدن دکمه‌های رنگی Telegram Bot API برای کیبوردهای Inline و Reply.
- رنگ‌بندی هوشمند دکمه‌ها: تأیید/خرید/پرداخت سبز، لغو/حذف/رد قرمز و مسیرها/تنظیمات آبی.
- اضافه شدن پنل `🎨 دکمه‌های رنگی` داخل `⚙️تنظیمات ربات کاربران`.
- امکان روشن/خاموش کردن دکمه‌های رنگی از داخل ربات بدون ویرایش `.env`.
- اضافه شدن طرح‌های آماده: `✨ هوشمند`، `🛒 فروشگاهی`، `💼 حرفه‌ای` و `🕊 مینیمال`.
- ذخیره ماندگار تنظیمات طرح دکمه‌ها در دیتابیس ربات.

## Hiddify-SellBot v2.2.0

Minor Release برای تکمیل لینک اشتراک هوشمند و مرتب‌سازی صفحه اصلی پروژه.

### تغییرات اصلی

- تکمیل لینک اشتراک اختصاصی با مسیر UUID کاربر: `/sub/<USER_UUID>/all.txt`.
- ارسال نام پروفایل به کلاینت با هدر `profile-title`.
- ارسال حجم مصرفی، حجم کل و زمان باقی‌مانده با هدر `subscription-userinfo`.
- محاسبه زنده مصرف از همه سرورها و نودهای همان سرویس هنگام بروزرسانی پروفایل.
- حفظ سازگاری با لینک‌های قدیمی توکن‌دار.
- بهبود README و انتقال تاریخچه نسخه‌ها به `CHANGELOG.md`.

## Hiddify-SellBot v2.1.14

- اجرای `bootstrap.sh` بدون آرگومان اکنون به‌صورت پیش‌فرض `install` را اجرا می‌کند.
- اضافه شدن مثال اجرای `panel` در bootstrap برای مدیریت دستی.
- شفاف‌سازی توضیحات Quick Start در README.

## Hiddify-SellBot v2.1.13

- اضافه شدن `bootstrap.sh` برای نصب هوشمند با یک دستور، حتی روی سرور خام بدون git.
- بروزرسانی Quick Start برای نصب ساده و مستقیم.
- امکان اجرای مستقیم `install`، `update` و `panel` از طریق bootstrap.

## Hiddify-SellBot v2.1.12

- بهبود حذف کاربران منقضی‌شده.
- بهبود جریان حذف کاربر و جلوگیری از نمایش غیرضروری لیست بعد از حذف.
- بهبود افزودن چند کاربر و افزودن کاربر با پلن.

## Hiddify-SellBot v2.1.11

- اضافه شدن گزینه `update-force` به منوی panel.
- ساده‌تر شدن آپدیت سخت برای همگام‌سازی کامل با GitHub.

## Hiddify-SellBot v2.1.10

- بهبود فرآیند آپدیت و نمایش نسخه بعد از بروزرسانی.
- رفع مشکل ماندن نسخه قدیمی در وضعیت ربات بعد از آپدیت.

## Hiddify-SellBot v2.1.9

- اضافه شدن حالت‌های مستقل تمدید برای حجم و زمان.
- امکان انتخاب اینکه حجم یا زمان هنگام تمدید اضافه شود یا ریست شود.

## Hiddify-SellBot v2.1.8

- بازگشت گزینه `ssl setup wizard` به منوی ساده `panel`.
- حفظ دسترسی سریع به SSL در کنار گزینه‌های اصلی نصب.

## Hiddify-SellBot v2.1.7

- ساده‌سازی منوی `panel` برای کاربران مبتدی.
- اولویت دادن به گزینه‌های اصلی: نصب، آپدیت، ریستارت، وضعیت و AutoStart.

## Hiddify-SellBot v2.1.6

- اضافه شدن AutoStart Manager داخل منوی نصب.
- مدیریت روشن/خاموش/وضعیت/حذف AutoStart از طریق منوی مرحله‌ای.

## Hiddify-SellBot v2.1.5

- اضافه شدن AutoStart واقعی با systemd.
- اتصال start/stop/restart/status به systemd در صورت نصب بودن سرویس‌ها.
- جلوگیری از اجرای همزمان nohup و systemd.

## Hiddify-SellBot v2.1.4

- محافظت از داده‌های runtime هنگام آپدیت.
- snapshot گرفتن از `Shared/servers.json` و `Shared/plans.json` قبل از git sync.
- restore کردن داده‌های runtime بعد از آپدیت.

## Hiddify-SellBot v2.1.3

- اضافه شدن ویزارد SSL داخل منوی `install.sh`.
- ساده‌تر شدن گرفتن SSL برای لینک‌های اشتراک هوشمند.

## Hiddify-SellBot v2.1.2

- بازطراحی منوی تعاملی `install.sh`.
- اضافه شدن دستورهای `update-force`، `reinstall`، `panel`، `diag` و `logs`.
- بهبود منطق update برای تغییرات runtime.

## Hiddify-SellBot v2.1.1

- اضافه شدن نام اشتراک داخل پیام‌های یادآوری تمدید.
- یکدست شدن پیام‌های یادآوری حجم و زمان.
- ارسال یادآور جداگانه برای هر اشتراک کاربر.

## Hiddify-SellBot v2.1.0

- اضافه شدن دستور `/debug` در AdminBot.
- رفع مشکل تنظیم دامنه لینک اشتراک هوشمند.
- پشتیبانی از دامنه با و بدون `https://`.
- اضافه شدن دستور `./install.sh ssl DOMAIN [EMAIL]`.
- پشتیبانی از حجم تست اعشاری مثل `0.2` و `0.5` گیگ.
- رفع مشکل ماندگاری کارت‌ها و تنظیمات در فایل‌های runtime.

## Hiddify-SellBot v2.0.0

Major Release با تمرکز روی پایداری عملیاتی، نودسازی، مدیریت دقیق اشتراک‌ها و رفع باگ‌های بحرانی.

- تکمیل منطق نودسازی و همگام‌سازی اشتراک بین سرور اصلی و نودها.
- فعال/غیرفعال‌سازی کاربر روی همه نودهای مرتبط.
- ویرایش حجم، زمان، نام و یادداشت روی سرور اصلی و نودها.
- بهبود Enforcer سراسری برای کنترل مصرف تجمیعی و قطع خودکار سرویس‌ها.
- بهبود نمایش سرویس‌ها در UserBot هنگام قطعی موقت پنل‌ها.
- حذف سرویس‌های حذف‌شده از پنل بعد از TTL.
- بازطراحی تیکت‌ها و مدیریت وضعیت تیکت.
- بهبود بکاپ، ریستور و آماده‌سازی پروژه برای انتشار تمیز در GitHub.
