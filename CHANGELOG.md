# Changelog

## SmsVerifierAndroid v1.5.2

### 🔍 تشخیص کامل اپ پیش‌فرض پیامک

- دکمه **«تشخیص کامل الزامات پیش‌فرض پیامک»** در بخش امنیت: گزارش کامل نسخه اندروید، اپ پیش‌فرض فعلی گوشی، وضعیت RoleManager، همه الزامات کاندید شدن (گیرنده‌های SMS_DELIVER / WAP_PUSH_DELIVER، سرویس CONFIGURATION، اکتیویتی SENDTO) و وضعیت مجوزهای رانتایم را با ✅/❌ نشان می‌دهد
- همین گزارش از داخل دیالوگ «فعال‌سازی انجام نشد» هم در دسترس است تا روی گوشی‌های خاص (مثل MIUI) دقیقاً مشخص شود کدام الزام رد شده است

---

## SmsVerifierAndroid v1.5.1

### 🆕 همگام‌سازی هوشمند

- **حذف نیاز به زدن دستی «همگام‌سازی»**: پیامک‌هایی که هنوز قطعی تایید نشده‌اند به‌صورت خودکار استعلام می‌شوند — هنگام باز شدن اپ (هر ۳ دقیقه حداکثر یک‌بار) و به‌صورت دوره‌ای در سرویس پس‌زمینه (هر ۱۰ دقیقه)
- اگر پرداخت بعداً در تلگرام تایید شود، رکورد خودکار به بخش «تایید شده» منتقل می‌شود و فقط مواردی که واقعاً مشکل دارند در «نیازمند بررسی» می‌مانند
- امن: رکوردهای تاییدشده و sms_reused هرگز دوباره استعلام نمی‌شوند
- تعداد پیامک‌های نمایش‌داده‌شده در بررسی تراکنش‌ها از ۱۵ به **۵۰** افزایش یافت

### 🐛 بهبود اندروید ۱۳ / شیائومی

- پیام «پیش‌فرض فعال نشد» که قبلاً Toast بریده‌شده بود، حالا **دیالوگ کامل مرحله‌به‌مرحله** با دکمه‌های «تلاش دوباره» و «تنظیمات پیش‌فرض» نمایش داده می‌شود (شامل چک نسخه اپ، مسیر Default apps و مجوز MIUI)
- اگر روم گوشی امکان RoleManager را محدود کرده باشد، پیام اختصاصی با مسیر دستی نشان داده می‌شود

---

## SmsVerifierAndroid v1.5.0

### 🐛 رفع باگ‌های اندروید ۱۲/۱۳

- **رفع دریافت نشدن پیامک روی اندروید ۱۲ به بالا**: افزودن سرویس `SmsConfigService` با اکشن `android.telephony.action.CONFIGURATION` و گیرنده `SmsSentReceiver` — بدون این‌ها اپ در لیست «برنامه پیامک پیش‌فرض» سیستم ظاهر نمی‌شد و دیالوگ فعال‌سازی RoleManager خطا می‌داد
- اپ حالا در تنظیمات Default apps ← SMS app و در پنجره رسمی RoleManager کاندید می‌شود و پس از انتخاب، پیامک‌ها با اکشن `SMS_DELIVER` دریافت و در صندوق ثبت می‌شوند
- هشدار «دسترسی کامل پیامک» حالا در بخش «بررسی تراکنش‌ها» به‌صورت خودکار فقط برای اندروید ۱۲+ نمایش داده می‌شود (اندروید ۱۱ به پایین این هشدار را نمی‌بیند چون نیازی ندارد)
- کارت «دسترسی کامل پیامک» در بخش امنیت هم فقط برای اندروید ۱۲+ ساخته می‌شود
- دیالوگ «اپ پیش‌فرض پیامک» هنگام همگام‌سازی صندوق فقط روی اندروید ۱۲+ ظاهر می‌شود (قبلاً روی ۱۰/۱۱ هم مزاحم بود)

---

## Hiddify-SellBot v5.2.0

### 🆕 قابلیت‌ها

- **ساخت سرور خودکار**: پروویژن سرور روی Hetzner و ثبت DNS روی Cloudflare از داخل ربات
- **چک سلامت سرورها** و گزارش خودکار قطعی
- **پرفکت مانی و ارز دیجیتال** به روش‌های پرداخت اضافه شد
- **وب‌هوک SMS بانکی**: تایید خودکار پرداخت کارت‌به‌کارت با اپ اندروید
- لینک اشتراک هوشمند با حذف خودکار کانفیگ‌های تکراری و مخفی + ردیف وضعیت مصرف

### 🔐 امنیت و پول

- **ضد دستکاری قیمت**: قیمت خرید و تمدید همیشه سمت سرور محاسبه می‌شود؛ دیتای دستکاری‌شده دکمه‌ها رد می‌شود (UserBot و CustomerBot)
- **ضد شارژ دوباره**: تایید همزمان پرداخت (وب‌هوک + ادمین) دیگر کیف پول را دوبار شارژ نمی‌کند
- **کسر اتمیک کیف پول** با refund خودکار در خطای ساخت سرویس
- **ذخیره اتمیک JSON**: کرش وسط نوشتن دیگر دیتای سرورها/پلن‌ها را پاک نمی‌کند
- توکن اشتراک با randomness رمزنگارانه (ضد brute-force)
- فایل‌های حساس (`.env`، دیتابیس، بکاپ‌ها) با دسترسی `600`

### 🔧 پایداری

- رفع فریز کامل CustomerBot در دریافت کانفیگ مستقیم
- رفع فریز UserBot در جمع‌آوری کانفیگ‌ها
- رفع `database is locked` با WAL و timeout روی همه دیتابیس‌ها
- رفع خطای cross-loop در فراخوانی پنل‌های X-UI از تردهای مختلف
- گارد اجرای همزمان enforcer (دستی + زمان‌بندی‌شده دیگر تداخل نمی‌کنند)
- invalidation کش X-UI برای همه سرورها و همه دامنه‌ها (بدون هاردکد)
- بعد از نصب SSL، پورت خام HTTP فقط روی localhost بایند می‌شود

### 📄 مستندات

- بازنویسی کامل README — فهرست ویژگی‌های کامل هر ۴ ربات

---

## Hiddify-SellBot v5.1.0

### 🆕 ساخت اینباند از لینک

- **ساخت اینباند از کانفیگ**: `vless://` / `vmess://` / `hysteria2://` / `trojan://` را پیست کن، ربات اینباند مشابه میسازد
- تشخیص خودکار دامنه و جایگزینی `direct.*` با دامنهٔ پنل، سوال پورت (0=همان، عدد=جدید)
- پشتیبانی از `ws` / `httpupgrade` / `grpc` / `xhttp` + `tls` / `reality` / `none` + `hysteria2`

### 🔧 فیکس‌های X-UI

- **همگام‌سازی یوزرها**: دکمه `🔄 همگام‌سازی یوزرها روی اینباندها` — یوزرهای قدیمی روی اینباند جدید ساخته میشن
- **TLS**: سرتیفیکت پنل (`/root/cert/...`) برای اینباندهای `tls` ست میشه (فیکس `both file and bytes are empty`)
- **پورت پنل**: جلوگیری از ساخت اینباند روی پورت خود پنل (443) + پیشنهاد پورت جایگزین
- **تایم‌اوت**: ریکاوری بعد از تایم‌اوت ساخت اینباند

### 🔧 بکاپ

- بکاپ ۶ ساعته برگشت به حالت **همهٔ سرورها با هم در یک فایل** (قبلاً فقط X-UI)

---

## Hiddify-SellBot v5.0.0

Major Release برای ادغام پنل X-UI و هوشمندسازی چنداینبانده.

### 🆕 پنل X-UI (3x-ui)

- **نوع سرور جدید X-UI** در افزودن سرور (در کنار هیدیفای) — پشتیبانی از forkهای `3x-ui` و `alireza0/x-ui`
- **حالت هوشمند چنداینبانده**:
  - `0` → همه اینباندهای فعال (هر چی الان داری + هر چی بعداً بسازی)
  - `1` → تک اینباند مشخص
  - `1,2,3` → چند اینباند مشخص با کاما
- UUID یکسان در همه اینباندهای انتخابی، `email` یونیک per inbound (`test` / `test-2`) و `subId` ثابت برای تجمیع سابسکریپشن
- `subId` ثابت میمونه تا لینک `/sub/uuid` همه کانفیگ‌ها (VLESS Reality, Trojan, VMess, Shadowsocks) رو جمع کنه
- **جمع ترافیک هوشمند**: `up+down` همه اینباندها جمع میشه، تمدید/فعال/غیرفعال/حذف روی همه میره
- **انتشار نام**: تغییر نام کاربر به همه اینباندها با الگوی `نام` و `نام-2` منتشر میشه

### 🔧 فیکس‌های ادمین و سرور

- **حذف پیام پیش‌نیاز** موقع افزودن سرور X-UI
- **فیکس آدرس پنل با base path**: `https://site.example.com/E6xNPh2XZF5A6UO` مستقیم تست میشه (قبلاً با API هیدیفای تست میشد)
- **فیکس تست اتصال X-UI** با `xui_api.test_connect` به جای `hiddify_api`
- **نمایش سرور X-UI**: `📦 پنل: X-UI` به جای `V11,12` + نمایش `یوزر پنل` و `اینباند`
- **منوی ویرایش X-UI**: `نام کاربری/رمز/دامنه ساب/اینباند` جای `کد مسیر ادمین/کاربران/کلید ادمین` هیدیفای
- **ساب‌اگریگیتور**: پشتیبانی از `xui_api` برای لینک‌های X-UI
- **دکمه بازگشت** در انتخاب نوع پنل + پاکسازی state

### 📱 اپ اندروید SmsVerifier 1.4.15

- درخواست نقش پیش‌فرض پیامک برای اندروید 12/13 (`RoleManager` + `Telephony.Sms`)
- مجوز پیامک اعلانی MIUI (`miui.intent.action.APP_PERM_EDITOR`) برای خواندن پیامک‌های بانکی
- فیکس‌های کامپایل (`DialogInterface`, stray brace) و `SMS_DELIVER` receiver

### 📦 فنی

- `Shared/xui_api.py` جدید (آداپتور کامل X-UI با API `/xui/API`)
- `Shared/hiddify_api.py` و `Shared/sub_aggregator.py` به‌روزرسانی برای X-UI

---

## Hiddify-SellBot v4.2.0

### 🆕 قابلیت‌های جدید

#### AgentBot (ربات نمایندگی)
- **تنظیمات عضویت اجباری**: بخش جدید در تنظیمات — فعال/غیرفعال کردن عضویت اجباری کانال، تنظیم کانال پشتیبانی، راهنمای کامل
- **دکمه ترتیب منوی اصلی ربات مشتری**: جابجایی دکمه خرید و تمدید اشتراک

#### CustomerBot (ربات مشتری)
- **همگام‌سازی تنظیمات لینک اشتراک با AdminBot**: تغییرات نوع لینک (هوشمند/مستقیم/...) توسط ادمین بلافاصله در ربات مشتری اعمال می‌شود
- **فیلتر نودها از لیست سرورها**: هنگام خرید اشتراک فقط سرورهای اصلی نمایش داده می‌شوند (نه نودهای فرزند)
- **جابجایی دکمه‌های خرید و تمدید**: دکمه تمدید اشتراک در سمت چپ و خرید اشتراک در سمت راست قرار گرفت

### 🔧 باگ‌های رفع شده

- **رفع مشکل restart**: هنگام restart ربات‌ها، AgentBot و CustomerBot که PID فایل‌شان باطل بود به‌درستی متوقف و مجدداً راه‌اندازی می‌شوند

---

## Hiddify-SellBot v4.0.0

Major Release برای ربات نمایندگی، ربات مشتری، و رفع باگ‌های حیاتی.

### 🆕 ربات‌های جدید

#### AgentBot (ربات نمایندگی)
- سیستم کامل مدیریت نمایندگان و مشتریان
- پنل مدیریت سفارشات و پرداخت‌ها
- سیستم کیف پول نماینده با کسر عمده
- مدیریت سرویس‌ها و تمدید اشتراک
- سیستم تیکت و پشتیبانی
- گزارش‌گیری فروش و درآمد

#### CustomerBot (ربات مشتری توسط نماینده)
- ثبت‌نام و ورود مشتریان
- خرید اشتراک از طریق نماینده
- مدیریت سرویس‌ها و لینک اشتراک
- سیستم کیف پول مشتری
- پشتیبانی از چند زبان

### 🔧 باگ‌های رفع شده (Critical)

#### UserBot
- **C-01**: رفع کرش PTB v20 با حذف `bootstrap_retries` و `close_loop`
- **C-02**: حذف fallback UUID جعلی در صورت خرابی API هیدیفای
- **C-03**: رفع مشکل صفحه‌بندی اشتراک‌های منقضی شده
- **C-04**: رفع race condition در نوشتن `.env` با `fcntl.flock`
- **C-05**: بازگشت هزینه از کیف پول در صورت خرابی پنل
- **C-06**: رفع مشکل جستجوی کاربران در پنل ادمین

#### AgentBot
- **H-03**: مدیریت خطا در `disable/enable/delete_subscription`
- **H-04**: رفع `__import__("datetime")` به import عادی
- رفع نشت اتصال دیتابیس (~30 تابع)
- رفع `__import__("random")` به `random.randint()`

#### CustomerBot
- **C-01**: حذف `handlers.py` قدیمی که با پکیج `handlers/` تداخل داشت
- **C-02**: رفع `__import__` هک به import عادی
- رفع race condition در `generate_order_id`

### 🛡️ بهبودهای امنیتی و پایداری

- **اتمیک کردن پرداخت‌ها**: حذف race condition TOCTOU در پرداخت مستقیم
- **حذف تکرار پرداخت**: حذف job queue تکراری که باعث ایجاد حساب‌های تکراری می‌شد
- **مدیریت خطا نودها**: در صورت خرابی نود، ادامه عملیات روی سایر نودها
- **مایگریشن ادمین**: دکمه جدید برای ثبت سرویس کاربران قدیمی
- **حذف BOM**: حذف کاراکتر BOM از 16 فایل پایتون
- **بکاپ کامل**: اضافه شدن `agency.db`، `customer_bot.db` و `agent_bot.db` به سیستم بکاپ/ریستور

### ⚡ بهبود عملکرد

- **کاهش تایم ضد اسپم**: از 8 ثانیه به 0.5 ثانیه برای دکمه "وضعیت اشتراک"
- **کاهش تایم کلی**: از 1.5 ثانیه به 0.5 ثانیه برای تمام دکمه‌ها
- **بهبود مدیریت حافظه**: استفاده از `try/finally` برای بستن اتصالات دیتابیس

### 📦 فایل‌های جدید

- `AgentBot/` - پوشه کامل ربات نمایندگی
- `CustomerBot/` - پوشه کامل ربات مشتری
- `Shared/agent_db.py` - دیتابیس نمایندگان
- `Shared/marzban_api.py` - API مارزبان
- `Shared/multi_panel.py` - مدیریت چند پنل
- `AdminBot/agencies.py` - ماژول آژانس‌ها

### 🧪 تست‌ها

- `test_customer_wallet_buy_pending_flow.py` - تست فلوی خرید کیف پول
- `test_userbot_replace_link.py` - تست جایگزینی لینک
- `test_node_user_propagation.py` - تست انتشار کاربر به نودها

### 📊 آمار تغییرات

- **فایل‌های تغییر یافته:** 85 فایل
- **فایل‌های جدید:** 10+ فایل/پوشه
- **تغییرات کد:** ~15,700 خط (+8,462 / -7,277)

### ⚠️ نکات مهم

- این نسخه دارای تغییرات ساختاری عمده است
- نیاز به مایگریشن دیتابیس دارد
- پشتیبانی از نسخه‌های قبلی متوقف می‌شود
- تست کامل قبل از استقرار در محیط Production ضروری است

---

## Hiddify-SellBot v3.2.1

Minor Release برای بهبود و توسعه سیستم تخفیف‌های دینامیکی پلن‌ها.

### تغییرات اصلی

- **تخفیف حجمی پلاکانی (Tiered Discount)**: سیستم جدید تخفیف‌های پله‌ای بر اساس حجم اشتراک
  - تعریف چندین سطح تخفیف (مثال: ۵۰GB با ۵٪، ۱۰۰GB با ۱۰٪، ۲۰۰GB با ۳۰٪)
  - فرمت ورودی انعطاف‌پذیر: `۵۰:۵, ۱۰۰:۱۰, ۲۰۰:۱۵` یا `50:5, 100:10, 200:15`
  - پشتیبانی کامل از اعداد فارسی و فاصله‌گذاری انعطاف‌پذیر
  - محاسبه خودکار بر اساس بالاترین سطح رسیده‌شده

- **سیستم خاموش/روشن مستقل**:
  - هر نوع تخفیف (ساده و پلاکانی) را می‌توان به‌صورت مستقل روشن/خاموش کرد
  - تنظیمات ذخیره‌شده هنگام خاموش کردن حفظ می‌شوند، نیازی به دوباره ورود نیست
  - دکمه‌های رنگی برای وضوح بیشتر: سبز برای فعال، قرمز برای غیرفعال

- **محاسبه بهترین تخفیف**:
  - وقتی هردو نوع تخفیف فعال باشند، بیشترین درصد تخفیف اعمال می‌شود
  - مثال: اگر تخفیف ساده ۳۰٪ و پلاکانی ۵٪ باشد، ۳۰٪ اعمال می‌شود

- **بهبود واجهه کاربری**:
  - منوی اختصاصی مدیریت تخفیف‌ها با نمایش وضعیت هر تخفیف
  - نمایش تنظیمات ذخیره‌شده برای تخفیف‌های غیرفعال
  - دکمه‌های ویرایش مستقل برای هر نوع تخفیف

- **تست‌های جامع**: ۱۱ تست واحد برای تحقق تمامی قابلیت‌های جدید

### نکات مهم

- تخفیف‌های قبلی (ساده) کاملاً پس‌وند ساز هستند و بدون تغییری کار می‌کنند
- می‌توانید هردو نوع تخفیف را همزمان تنظیم کنید و بهترین یک اعمال می‌شود
- اعداد فارسی برای ورود تخفیف‌ها مکاملاً پشتیبانی می‌شوند

## Hiddify-SellBot v3.0.0

Stable Release برای جمع‌بندی فیکس‌های پرداخت خودکار، نودسازی، لینک اشتراک هوشمند و ابزار نصب.

### تغییرات اصلی

- پایدارسازی تایید خودکار کارت‌به‌کارت با اپ SMS Verifier و Webhook اختصاصی.
- پشتیبانی امن‌تر از بانک‌هایی که ۴ رقم کارت مبدا را داخل SMS ارسال نمی‌کنند، مثل بلو بانک.
- حفظ منطق دقیق ۴ رقم کارت برای بانک‌هایی که آن را ارسال می‌کنند، مثل بانک خاورمیانه.
- جلوگیری از تایید دوباره SMS استفاده‌شده و کاهش ریسک رسیدهای فیک یا تکراری.
- همگام‌سازی بهتر وضعیت پیامک‌های زرد/سبز داخل اپ بعد از تایید پرداخت توسط ربات.
- بهبود گزارش‌های ادمین برای پرداخت‌های تاییدشده با SMS و مدیریت رسید پیوست‌شده.
- پایدارسازی لینک اشتراک هوشمند، QR، b64 و محاسبه مصرف چندسروره.
- تکمیل فیکس‌های نودسازی، نمایش منوها و عملیات کاربران/سرورها.
- بهبود ابزار نصب، آپدیت امن، `update-force`، AutoStart و SSL Wizard.
- تثبیت نسخه برای استفاده عملیاتی به‌عنوان نسخه پایدار جدید.

## Hiddify-SellBot v2.2.2

Patch Release برای تکمیل نودسازی، پایداری لینک اشتراک هوشمند و رفع چند باگ عملیاتی.

### تغییرات اصلی

- تکمیل و پایدارتر شدن همگام‌سازی نودها و عملیات چندسروره.
- رفع باگ‌های مرتبط با نمایش منوها و عملیات کاربران.
- بهبود پایداری کلی سیستم.

## Hiddify-SellBot v2.2.1

Patch Release برای بهبود ظاهر دکمه‌ها و رفع باگ‌های جزئی.

### تغییرات اصلی

- اضافه شدن تم رنگی به دکمه‌ها.
- رفع باگ‌های جزئی در مدیریت کاربران.

## Hiddify-SellBot v2.2.0

Minor Release برای پشتیبانی از اشتراک چندسروره.

### تغییرات اصلی

- پشتیبانی از اشتراک‌های چندسروره.
- بهبود مستندات نصب و راه‌اندازی.

## Hiddify-SellBot v2.1.14

Patch Release برای بهبود اسکریپت نصب.

### تغییرات اصلی

- بهبود اسکریپت نصب bootstrap.
- رفع باگ‌های جزئی.

## Hiddify-SellBot v2.1.13

Patch Release برای بهبود اسکریپت نصب.

### تغییرات اصلی

- اضافه شدن اسکریپت نصب یک خطی.
- بهبود مستندات.
